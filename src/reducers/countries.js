const initialState = {
  name: 'country name',
  capital: 'capital',
  region: 'region',
  population: 'population',
  language: [],
  flag: 'flag'
};

export default function reducer(state = initialState, action) {
  return state;
}
